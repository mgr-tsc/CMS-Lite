// Authentication module exports
export { AuthContext, AuthProvider, useAuth } from './auth'

// Re-export auth types for convenience
export type { User, AuthContextType, LoginResponseSuccess } from './auth'